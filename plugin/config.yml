# Plugin configuration
---
package:
  author: KBase Developer
  name: example-hello
  description: An example plugin which says "hello"
  type: iframe
install:
  routes:
    - path: dashboard4
      view: main
      authorization: true
  menu:
    - name: dashboard4
      definition:
        path: ["dashboard4"]
        label: Erik's Dashboard (V4)
        icon: tachometer
